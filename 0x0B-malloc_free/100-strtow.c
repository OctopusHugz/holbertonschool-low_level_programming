#include "holberton.h"

/**
 * strtow - splits a string into two words
 * @str: string to split
 *
 * Return: pointer to an array of strings
 */

char **strtow(char *str)
{
	if (str == NULL)
		return (NULL);

	return (0);
}
