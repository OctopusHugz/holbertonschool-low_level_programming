#include "holberton.h"

/**
 * add - adds 2 numbers together
 * @a: first number passed as argument
 * @b: second number passed as argument
 *
 * Return: 0 if success
 */

int add(int a, int b)
{
	return (a + b);
}
