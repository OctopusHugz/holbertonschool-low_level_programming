#include "holberton.h"

/**
 * _atoi - translates ascii value to integer
 * @s: string/char to turn into integer
 *
 * Return: integer value of s
 */

int _atoi(char *s)
{
	if (*s)
		s++;
	return (0);
}
