#include "holberton.h"

/**
 * mul - multiplies two integers
 * @a: first integer passed as argument to function to multiply
 * @b: second integer passed as argument to funciton to multiply
 *
 * Return: product of multiplying the two integers
 */

int mul(int a, int b)
{
	return (a * b);
}
